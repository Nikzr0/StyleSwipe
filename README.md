# StyleSwipe - Дигитален Гардероб

**Студент:** Никола Пепелов
**Фак. номер:** 2301321028
**Дисциплина:** Мобилни приложения 2025

---

## 1. Идея на проекта
**StyleSwipe** е модерно Android приложение, което помага на потребителите да организират и дигитализират личния си гардероб. Вдъхновено от популярните "Swipe" интерфейси, приложението позволява лесно разглеждане на наличните дрехи, добавяне на нови чрез камера или галерия и управление на детайли като марка, размер и цена.

Основната цел е да се реши проблемът "Нямам какво да облека", като се предостави визуален каталог на притежаваните дрехи.

---

## 2. Как работи (Функционалности)
Приложението предоставя интуитивен потребителски интерфейс с следните възможности:

* **Визуализация (Tinder-style):** Дрехите се показват като интерактивни карти. Потребителят може да разглежда снимките на дадена дреха чрез вградена галерия (стрелки ляво/дясно).
* **Добавяне на съдържание (Create):**
    * Въвеждане на данни: Име, Марка, Размер, Цена.
    * **Мултимедия:** Интеграция с **Камерата** за директно снимане и **Галерията** за избор на множество съществуващи снимки.
* **Редакция (Update):** Възможност за промяна на данните и добавяне/премахване на снимки към вече създадена дреха.
* **Управление (Delete):** Възможност за изтриване на дрехи от гардероба.

---

## 3. Архитектура и Технологии
Проектът е разработен, спазвайки стриктна **MVVM (Model-View-ViewModel)** архитектура и **Clean Code** принципи.

* **Език:** Kotlin
* **UI:** Jetpack Compose (Material 3 Design)
* **Архитектура:** MVVM + Repository Pattern
* **База данни (Local DB):** Room Database (SQLite)
* **Асинхронност:** Kotlin Coroutines & Flow
* **Зареждане на изображения:** Coil
* **Хардуерна интеграция:** Android Camera Intent, FileProvider, Gallery Picker

### Структура на пакетите:
* `data`: Room Entities, DAO, Database, Repository, TypeConverters.
* `ui/screens`: Екраните на приложението (Home, AddClothing).
* `viewmodel`: Бизнес логиката и управлението на състоянието (State).

---

## 4. Потребителски поток (User Flow)
1.  **Начален екран:** Показва се "празен" екран или последната добавена карта.
2.  **Добавяне:** Натискане на бутона `+` отваря екрана за добавяне.
3.  **Въвеждане:** Потребителят попълва детайли и избира снимки (чрез Камера или Галерия).
4.  **Запис:** При натискане на "Запази", данните се съхраняват локално в базата.
5.  **Интеракция:** На началния екран потребителят може да редактира (молив), изтрива (кошче) или споделя (сърце) дрехата.

---

## 5. Стъпки за стартиране
1.  Клонирайте хранилището.
2.  Отворете проекта в **Android Studio Ladybug** (или по-нова).
3.  Изчакайте Gradle Sync да завърши.
4.  Стартирайте на емулатор (Препоръчително: **Pixel 7, API 35**).
    * *Забележка:* При първо пускане разрешете достъп до камерата/файловете при поискване.

---

## 6. Тестови акаунти
Приложението използва локална база данни (Offline-first), поради което **не се изисква регистрация или вход**. Всички данни се пазят на устройството.

---

## 7. APK Файл
Готовият за инсталация файл се намира в root директорията на хранилището:
📂 **`/apk/app-release.apk`**

---

## 8. Тестове и Качество на кода
* **Unit Tests:** Покритие на логиката за конвертиране на данни (`ConvertersTest`) и интегритет на модела (`ClothingItemTest`).
* **UI Tests:** Espresso тест за навигация и наличие на UI елементи (`AppNavigationTest`).
* Code Style: Спазени са Kotlin конвенциите.